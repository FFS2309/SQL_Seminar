-- 68.
CREATE VIEW "Mitarbeiter_City" as SELECT FIRST_NAME, LAST_NAME, CITY FROM EMPLOYEES, LOCATIONS, DEPARTMENTS WHERE EMPLOYEES.DEPARTMENT_ID = DEPARTMENTS.DEPARTMENT_ID AND LOCATIONS.LOCATION_ID = DEPARTMENTS.LOCATION_ID;